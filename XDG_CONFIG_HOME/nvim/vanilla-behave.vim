" NeoVIM Configuration
" Made by ccxex29
" Version 2020/12

" Enable Persistent Undo History
set undofile

" Do not show additional mode on the status bar, since there's already a plugin for that
set noshowmode
" Set LastStatusBar Position to 2
set laststatus=2
set cmdheight=1

" Non-assumptive case insensitivity
set ignorecase
set nosmartcase

" Change indentation to 4 (shiftwidth) div 4 (tabstop) = 1 (one tab as 4 spaces) 
set tabstop=4
set shiftwidth=4
